@import '../../variable.pcss';

.footer {
  background-color: var(--primary-background-color);
  bottom: 0;
  color: var(--text-primary-color);
  font-size: 12px;
  padding: 16px;
  position: fixed;
  width: 100%;
}
